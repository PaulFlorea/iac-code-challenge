name: CI/CD

on:
  workflow_dispatch: {}
  push: {}
  #   branches:
  #     - main

jobs:
  test_and_plan:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      # TODO: Cleanup
      # - name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v1

      # - name: Login to GitHub Container Registry
      #   uses: docker/login-action@v1
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.repository_owner }}
      #     password: ${{ secrets.GHRC_TOKEN }}

      # - name: Build and push
      #   uses: docker/build-push-action@v2
      #   with:
      #     context: .
      #     file: ./Dockerfile
      #     push: true
      #     tags: |
      #       jamtur01/animal-farm-nodejs:latest
      #       ghcr.io/jamtur01/animal-farm-nodejs:latest

      - name: Init Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.4.6

      - name: Check Formatting
        run: terraform fmt -list=true -check -recursive -diff && echo "Terraform formatting is valid."

      # TODO: configure
      - id: 'auth'
        name: 'Authenticate to Google Cloud'
        uses: 'google-github-actions/auth@v1'
        with:
          workload_identity_provider: 'projects/628519435158/locations/global/workloadIdentityPools/my-pool/providers/my-provider'
          service_account: 'terraform-provisioner@coherent-flame-385415.iam.gserviceaccount.com'